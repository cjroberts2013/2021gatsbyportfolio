import * as React from "react"
import Layout from './../components/Layout'
import Heading from "./../components/Heading"

const Index = () => {
  return (
    <Layout>
      <Heading header="I create beautiful websites that get results" preheading="Results Based Design" cta="Get in touch today" ctalink="/contact" />
    </Layout>
  )
}

export default Index
